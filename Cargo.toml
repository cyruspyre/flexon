[package]
name = "flexon"
version = "0.1.2"
edition = "2024"

[profile.release]
lto = true

[dev-dependencies]
criterion = "0.7.0"

[lib]
bench = false

[[bench]]
name = "parse"
harness = false

[features]
default = ["comment"]
comment = []
metadata = ["comment"]
